import { useState } from 'react'
const API=import.meta.env.VITE_API_URL||'/api'
export default function ContactForm(){const [form,setForm]=useState({name:'',email:'',message:''}),[sent,setSent]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState('');async function submit(e){e.preventDefault();setBusy(true);setError('');try{const r=await fetch(`${API}/inquiries`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});if(!r.ok)throw Error();setSent(true);setForm({name:'',email:'',message:''})}catch{setError('Could not send right now. Please start the backend and try again.')}finally{setBusy(false)}}if(sent)return <div className="form-success"><span>✓</span><h3>Message received.</h3><p>Your inquiry is now available in the admin panel.</p><button onClick={()=>setSent(false)}>Send another ↗</button></div>;return <form className="contact-form" onSubmit={submit}><label>Name<input value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="Your name" required/></label><label>Email<input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="you@company.com" required/></label><label>Project details<textarea value={form.message} onChange={e=>setForm({...form,message:e.target.value})} placeholder="Tell us what you want to build..." required/></label>{error&&<p className="form-error">{error}</p>}<button className="form-submit" disabled={busy}>{busy?'Sending…':'Send inquiry'} <span>↗</span></button></form>}
